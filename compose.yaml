services:
  redis:
    image: redis:7
    restart: unless-stopped
    ports:
      - "6379:6379"

  chromadb:
    image: chromadb/chroma:0.6.3
    restart: unless-stopped
    ports:
      - "8001:8000"
    volumes:
      - ./data/chroma:/chroma/chroma
